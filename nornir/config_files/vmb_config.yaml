---
inventory:
    plugin: SimpleInventory
    options:
        host_file: "./vmb_inv/hosts.yaml"
        group_file: "./vmb_inv/groups.yaml"
        defaults_file: "./vmb_inv/defaults.yaml"

logging:
    enabled: True

runner:
    plugin: threaded
    options:
        num_workers: 20