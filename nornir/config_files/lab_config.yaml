---
inventory:
    plugin: SimpleInventory
    options:
        host_file: "./lab_inventory/hosts.yaml"
        group_file: "./lab_inventory/groups.yaml"
        defaults_file: "./lab_inventory/defaults.yaml"

logging:
    enabled: True

runner:
    plugin: threaded
    options:
        num_workers: 20