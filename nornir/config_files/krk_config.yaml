---
inventory:
    plugin: SimpleInventory
    options:
        host_file: "./krk_inv/hosts.yaml"
        group_file: "./krk_inv/groups.yaml"
        defaults_file: "./krk_inv/defaults.yaml"

logging:
    enabled: True

runner:
    plugin: threaded
    options:
        num_workers: 20