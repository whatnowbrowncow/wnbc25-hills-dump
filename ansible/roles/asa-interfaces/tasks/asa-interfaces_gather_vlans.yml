---

#- name: Debugging {{ context_item.context }}
#  debug:
#    msg: "{{ context_item.vlans }}"
#    
    
- name: Gather interfaces for {{ context_item.context }}
  include_tasks: ./asa-interface_builder.yml
  with_items:
    - "{{ context_item.vlans }}"
  loop_control:
    loop_var: context_interfaces_item
    
