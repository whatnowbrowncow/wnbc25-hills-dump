---

- name: Generate SNMP configurations
  template:
    src: "snmp.j2"
    dest: "/home/ubuntu/vagrant_data/ansible/roles/core/configs/{{ inventory_hostname }}/snmp.cfg"
  tags:
    - snmp

